<section class="about-container">
  <div class="about-content">
    <!-- Left Side: Text -->
    <div class="about-text">
      <span class="badge">OUR VISION</span>
      <h2>Turn your ideas into reality.</h2>
      <p class="description">Capitalize on low hanging fruit to identify a ballpark value-added activity beta test. Override the digital divide with additional from DevOps.</p>
      <div class="features">
        <div class="feature-item">
          <span class="check-icon">✔</span>
          <p>Bring to the table win-win survival strategies to ensure proactive domination. At the end of the day.</p>
        </div>
        <div class="feature-item">
          <span class="check-icon">✔</span>
          <p>Capitalize on low hanging fruit to identify a ballpark value added activity to beta test override the digital.</p>
        </div>
      </div>
    </div>

    <!-- Right Side: Image -->
    <div class="about-image">
      <div class="green-box"></div>
      <img src="/images/photoo.jpg" alt="Hero Image" />
      <div class="pattern"></div>
    </div>
  </div>

  <!-- Statistics Section -->
  <div class="stats">
    <div class="stat-item">
      <h3>1128+</h3>
      <p>Successful Work</p>
    </div>
    <div class="stat-item">
      <h3>908+</h3>
      <p>Team Member</p>
    </div>
    <div class="stat-item">
      <h3>258+</h3>
      <p>Happy Customers</p>
    </div>
    <div class="stat-item">
      <h3>564+</h3>
      <p>Creative Ideas</p>
    </div>
  </div>
</section>
<style>
  /* General Styling */
  body {
    font-family: Arial, sans-serif;
    background: #fff;
    margin: 0;
    padding: 0;
  }

  /* About Section Container */
  .about-container {
    max-width: 1200px;
    margin: auto;
    padding: 60px 20px;
  }

  /* Content Layout */
  .about-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  /* Left Side: Text */
  .about-text {
    max-width: 50%;
  }

  .badge {
    background: #4caf50;
    color: white;
    padding: 8px 14px;
    font-size: 14px;
    font-weight: bold;
    border-radius: 20px;
  }

  h2 {
    font-size: 36px;
    font-weight: bold;
    margin: 20px 0;
  }

  .description {
    color: #555;
    line-height: 1.6;
  }

  /* Feature List */
  .features {
    margin-top: 20px;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 10px;
  }

  .check-icon {
    font-size: 20px;
    color: #4caf50;
    font-weight: bold;
  }

  /* Right Side: Image */
  .about-image {
    position: relative;
    width: 500px;
  }

  .about-image img {
    width: 100%;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
  }

  .green-box {
    position: absolute;
    top: -15px;
    right: -30px;
    width: 160px;
    height: 160px;
    background: #4caf50;
    border-radius: 10px;
    z-index: -1;
  }

  .pattern {
    position: absolute;
    top: -30px;
    left: -140px;
    width: 400px;
    height: 400px;
    background: url("pattern.png") no-repeat center;
    background-size: cover;
    z-index: -2;
  }

  /* Statistics Section */
  .stats {
    display: flex;
    justify-content: space-around;
    text-align: center;
    margin-top: 50px;
  }

  .stat-item h3 {
    font-size: 28px;
    font-weight: bold;
  }

  .stat-item p {
    color: #777;
    font-size: 14px;
  }

  /* Responsive Design */
  @media (max-width: 992px) {
    .about-content {
      flex-direction: column;
      text-align: center;
    }

    .about-text {
      max-width: 100%;
    }

    .about-image {
      width: 100%;
    }

    .stats {
      flex-wrap: wrap;
      gap: 20px;
    }

    .stat-item {
      flex: 1;
    }
  }
</style>
